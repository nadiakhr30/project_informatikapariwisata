# Sistem Rekomendasi Wisata Indonesia

Aplikasi rekomendasi wisata berbasis Streamlit menggunakan TF-IDF dan cosine similarity.

## Menjalankan aplikasi

```bash
pip install -r requirements.txt
streamlit run app.py
```

Data utama dibaca dari `wisata_indonesia_final.csv`.
